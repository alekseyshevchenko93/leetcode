
const compare = (a, b) => {
  return a.toUpperCase() === b;
};

function nutsAndBolts(arr1, arr2, fn) {
  return arr1.sort((a, b) => {
    // todo
  });
}

console.log(nutsAndBolts(['b', 'a', 'c', 'd'], ['D', 'A', 'C', 'B']));

// brute force: n^2

